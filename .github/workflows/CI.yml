name: CI

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  run_tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Check Kernel Config (Rust)
        run: cat /boot/config-$(uname -r) | grep _RUST

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: cargo dkms
          version: 1.0

      - name: Build module
        working-directory: abuse-kmod
        run: sudo make install
      
      - name: Load module
        run: |
          sudo modprobe abuse
          ls /dev

      - name: Install abusectl
        run: sudo cargo install --path abusectl --root /usr/local

      - name: Add /dev/abuse1
        run: |
          sudo abusectl add 1 
          ls /dev
          lsblk

      - name: Connect /dev/abuse1 and ramdisk
        run: |
          sudo cargo build -p abuse-ramdisk --release
          sudo target/release/abuse-ramdisk 1 > /var/abuse.log &
          sleep 1
          lsblk

      - name: Logical test (badblocks)
        run: sudo badblocks -wsv /dev/abuse1

      - name: Cat make.log
        run: |
          sudo cat -q /var/lib/dkms/abuse/1.0.0/build/make.log
          sudo cat -q /var/abuse.log
        if: ${{ failure() }}